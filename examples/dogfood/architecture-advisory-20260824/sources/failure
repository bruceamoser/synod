A long-lived service with an in-process queue risks losing in-flight work on a crash unless the queue is persisted. Mitigations: persist the queue to a database table or a durable queue, and add a dead-letter path for items that fail repeatedly. A per-batch job model gives natural failure isolation (a failed job loses only that batch) but needs a reaper to detect and re-enqueue stuck jobs.
